/* *******************************************************
    PROGRAMA DE TESTE COM TODOS OS ELEMENTOS DA LINGUAGEM
   ******************************************************* */

pflut global;

func fatorial(int x)(int) {
  int variavel;
  int aux[2];
  bool compara;

  aux := 80+(2+3*4)/2-(4+2)*7*5-1;
  compara := cond;

  se(cond) {
    variavel := 5;
    variavel := aux[1];
  } senao {
    variavel := fatorial(3);
  }

  enquanto(cond) {
    fatorial(9);
  }

  para (i := 0; cond; i := 5) {
    variavel := 6;
  }
}

func main()() {

  int num[6];
  int i;
  bool expBool;

  // Código apenas para teste de expressões
  i := 2+4*4-5*4**2;

  // Preenche array de 1 a 6
  para (i = 0; cond; i := i+1) {
    num[i] := i+1;
  }

  enquanto (cond) {
    num[i] := fatorial(num[i]);
    i = i-1;
  }
}
